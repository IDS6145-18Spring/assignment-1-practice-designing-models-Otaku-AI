class Agent:
	
	def __init_(self, destination,adaptability, priority):
		self.destination = destination #where the user is headed
		self.adaptability = adaptability #how willing they are to change their current plans
		self.priority = priority #what priority they have in the queue
	
	#sends a message to the system
	def sendMessage(string):
		return
	
	#This function receives messages for the system
	def receiveMessage():
	#Initialize empty message
	currentMessage = ''
		return currentMessage
	
	#Returns results of utility function
	def getUtility():
		return 
